services:
  - type: web
    name: school-backend
    runtime: node
    region: oregon
    plan: starter
    buildCommand: cd back && npm install && npx prisma generate && npm run build
    startCommand: cd back && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
      - key: FRONTEND_URL
        value: https://grp-ashy.vercel.app
      - key: DATABASE_URL
        sync: false  # Set this in Render dashboard
      - key: JWT_SECRET
        generateValue: true
    healthCheckPath: /api/health
